#  - binary_sensor:
 #     - name: UDMP WAN1 State
 #       icon: mdi:ethernet
  #      #unit_of_measurement: ""
#        state: >
#          {%- set line = states('sensor.udmp_wan_state').split('wanFailover')[2].split(" ") -%}
 #         {%- if line | count > 4 %}
#            {{- line[11] == 'up' }}
 #         {%- else %}
  #          off
  #        {%- endif %}
  
 #     - name: UDMP WAN2 State
 #       icon: mdi:signal-cellular-2
 #       #unit_of_measurement: ""
 #       state: >
  #        {%- set line = states('sensor.udmp_wan_state').split('wanFailover')[2].split(" ") -%}
  #        {%- if line | count > 4 %}
   #         {{- line[7] == 'up' }}
   #       {%- else %}
    #        off
     #     {%- endif %}
          
  # [2] - active
  # [7] - eth8 state - WAN RJ45 Socket (LTE Modem)
  # [11] - eth9 state - WAN2 SFP+ Socket (ISP cable)

  
  - binary_sensor:
      - name: USW16 Fan State
        state: >
          {{ states('sensor.usw16_fan_speed')|int(0) > 0 }}
        attributes:
          friendly_name: USW15 Fan State
          icon: mdi:fan
  #     - name: UDM Upgradable
  #       state: >
  #         {{ states('sensor.udm_unifios').split('|\n')[6] | default }}
  #       attributes:
  #         friendly_name: UDM Upgradable
  #         icon: mdi:update
  #         device_class: update
