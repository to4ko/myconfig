# ############################################################################
# Hardware sensors
# ############################################################################
  # - platform: statistics
  #   entity_id: sensor.pmox01_nvme0_session_write
  #   name: pmox01 NVME0 Write Change 1h
  #   state_characteristic: change
  #   sampling_size: 20000
  #   max_age:
  #     minutes: 60

  # - platform: statistics
  #   entity_id: sensor.pmox01_sda_session_write
  #   name: pmox01 SDA Write Change 1h
  #   state_characteristic: change
  #   sampling_size: 20000
  #   max_age:
  #     minutes: 60

  - platform: statistics
    entity_id: sensor.pbs100_nvme0_session_write
    name: pbs100 NVME0 Write Change 1h
    state_characteristic: change
    sampling_size: 20000
    max_age:
      minutes: 60

  - platform: statistics
    entity_id: sensor.pbs100_sda_session_write
    name: pbs100 SDA Write Change 1h
    state_characteristic: change
    sampling_size: 20000
    max_age:
      minutes: 60

  - platform: statistics
    unique_id: ms01a_nvme0_session_write_change_1h
    entity_id: sensor.ms01a_nvme0_session_write
    name: ms01a NVME0 Write Change 1h
    state_characteristic: change
    sampling_size: 20000
    max_age:
      minutes: 60

  - platform: statistics
    unique_id: ms01a_nvme1_session_write_change_1h
    entity_id: sensor.ms01a_nvme1_session_write
    name: ms01a NVME1 Write Change 1h
    state_characteristic: change
    sampling_size: 20000
    max_age:
      minutes: 60
      
  - platform: statistics
    unique_id: ms01b_nvme0_session_write_change_1h
    entity_id: sensor.ms01b_nvme0_session_write
    name: ms01b NVME0 Write Change 1h
    state_characteristic: change
    sampling_size: 20000
    max_age:
      minutes: 60

  - platform: statistics
    unique_id: ms01b_nvme1_session_write_change_1h
    entity_id: sensor.ms01b_nvme1_session_write
    name: ms01b NVME1 Write Change 1h
    state_characteristic: change
    sampling_size: 20000
    max_age:
      minutes: 60

  - platform: statistics
    unique_id: ms02_nvme0_session_write_change_1h
    entity_id: sensor.ms02_nvme0_session_write
    name: ms02 NVME0 Write Change 1h
    state_characteristic: change
    sampling_size: 20000
    max_age:
      minutes: 60

  - platform: statistics
    unique_id: ms02_nvme1_session_write_change_1h
    entity_id: sensor.ms02_nvme1_session_write
    name: ms02 NVME1 Write Change 1h
    state_characteristic: change
    sampling_size: 20000
    max_age:
      minutes: 60