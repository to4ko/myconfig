# ############################################################################
# Hardware sensors
# ############################################################################
  - platform: statistics
    entity_id: sensor.pmox01_nvme0_session_write
    name: pmox01 NVME0 Write Change 1h
    state_characteristic: change
    sampling_size: 20000
    max_age:
      minutes: 60

  - platform: statistics
    entity_id: sensor.pmox01_sda_session_write
    name: pmox01 SDA Write Change 1h
    state_characteristic: change
    sampling_size: 20000
    max_age:
      minutes: 60

  - platform: statistics
    entity_id: sensor.pmox02_nvme0_session_write
    name: pmox02 NVME0 Write Change 1h
    state_characteristic: change
    sampling_size: 20000
    max_age:
      minutes: 60

  - platform: statistics
    entity_id: sensor.pmox02_sda_session_write
    name: pmox02 SDA Write Change 1h
    state_characteristic: change
    sampling_size: 20000
    max_age:
      minutes: 60
