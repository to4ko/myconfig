# ##################################
# System - Breather Control Alice
# ##################################
  - alias: system_breather_control_alice
    id: system_breather_control_ alice
    initial_state: true
    mode: parallel
    trigger:
      - platform: event
        event_type: yandex_intent
        event_data:
          text: Выключи вентиляцию
        id: 'off'
      - platform: event
        event_type: yandex_intent
        event_data:
          text: Включи вентиляцию
        id: 'on'
    condition:
      - condition: state
        entity_id: switch.security_mode
        state: 'off'
      - condition: template
        value_template: "{{ trigger.event.data.entity_id != 'media_player.yandex_v' }}"
    action:
      - choose:
          - conditions:
              - condition: template
                value_template: "{{ trigger.event.data.entity_id == 'media_player.yandex_mb' }}"
            sequence:
              - service: 'switch.turn_{{ trigger.id }}'
                entity_id: switch.breather_mb_last_toggle_by_user
              - service: 'fan.turn_{{ trigger.id }}'
                entity_id: fan.breather_mb
          - conditions:
              - condition: template
                value_template: "{{ trigger.event.data.entity_id == 'media_player.yandex_s' }}"
            sequence:
              - service: 'switch.turn_{{ trigger.id }}'
                entity_id: switch.breather_s_last_toggle_by_user
              - service: 'fan.turn_{{ trigger.id }}'
                entity_id: fan.breather_s

# ##################################
# System - Breather Control User
# ##################################
  - alias: system_breather_control_user
    id: system_breather_control_user
    initial_state: true
    mode: parallel
    trigger:
      - platform: state
        entity_id: fan.breather_mb
        to: 'off'
        id: 'id_state_mb'
      - platform: state
        entity_id: fan.breather_s
        to: 'off'
        id: 'id_state_s'
      # - platform: state
      #   entity_id: switch.breather_auto_mb, switch.breather_auto_v, switch.breather_auto_s
      #   to: 'on'
      #   id: 'id_state_auto_on'
    condition:
      - condition: state
        entity_id: switch.security_mode
        state: 'off'
    action:
      - choose:
          - conditions:
              - condition: trigger
                id: 'id_state_mb'
            sequence:
              - service: switch.turn_off
                entity_id: switch.breather_mb_last_toggle_by_user
          - conditions:
              - condition: trigger
                id: 'id_state_s'
            sequence:
              - service: switch.turn_off
                entity_id: switch.breather_s_last_toggle_by_user
          # - conditions:
          #     - condition: trigger
          #       id: 'id_state_auto_on'
          #   sequence:
          #     - service: 'switch.turn_{{ states("switch.breather_auto_s") }}'
          #       entity_id: switch.switch.breather_s_last_toggle_by_user
          #     - service: 'switch.turn_{{ states("switch.breather_auto_mb") }}'
          #       entity_id: switch.switch.breather_mb_last_toggle_by_user
          #     - service: 'switch.turn_{{ states("switch.breather_auto_v") }}'
          #       entity_id: switch.switch.breather_v_last_toggle_by_user