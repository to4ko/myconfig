  # - platform: rest
  #   resource: https://edge.qiwi.com/sinap/crossRates
  #   name: QIWI USD Rate
  #   #force_update: true
  #   headers: 
  #     authorization: !secret qiwi_token
  #     Content-Type: application/json
  #     Accept: application/json
  #   value_template: "{{ value_json.result | selectattr('to', 'eq', '840') | selectattr('from', 'eq', '643') | map(attribute='rate') | join('') }}"
  #   unit_of_measurement: RUB

  # - platform: rest
  #   resource: https://edge.qiwi.com/sinap/crossRates
  #   name: QIWI YEN Rate
  #   #force_update: true
  #   headers: 
  #     authorization: !secret qiwi_token
  #     Content-Type: application/json
  #     Accept: application/json
  #   value_template: "{{ value_json.result | selectattr('to', 'eq', '156') | selectattr('from', 'eq', '643') | map(attribute='rate') | join('') }}"
  #   unit_of_measurement: RUB          