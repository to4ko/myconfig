  
  # tv_v_toggle:
  #   alias: Sony TV OnOff
  #   sequence:
  #     - service: remote.send_command
  #       entity_id: remote.mi_remote
  #       data:
  #         command:
  #           - 'sony_pwr'

  # iconbit_toggle:
  #   alias: Iconbit OnOff
  #   sequence:
  #     - service: remote.send_command
  #       entity_id: remote.mi_remote
  #       data:
  #         command:
  #           - 'iconbit_pwr'

  # kodi_v_toggle:
  #   alias: Kodi V OnOff
  #   sequence:
  #     - service: remote.send_command
  #       entity_id: remote.mi_remote
  #       data:
  #         command:
  #           - 'kodi_v_pwr'

  # breather_v_toggle:
  #   alias: Breather V OnOff
  #   sequence:
  #     - service: remote.send_command
  #       entity_id: remote.mi_remote
  #       data:
  #         command:
  #           - 'breather_pwr'
            
  # breather_v_up:
  #   alias: Breather V Up
  #   sequence:
  #     - service: remote.send_command
  #       entity_id: remote.mi_remote
  #       data:
  #         command:
  #           - 'breather_v_up'
            
  # breather_v_down:
  #   alias: Breather V Down
  #   sequence:
  #     - service: remote.send_command
  #       entity_id: remote.mi_remote
  #       data:
  #         command:
  #           - 'breather_v_down'

  breather_v_toggle:
    alias: Breather V OnOff
    sequence:
      - service: switch.turn_on
        entity_id: switch.digma_ir_v_breather_pwr
        

  breather_s_toggle:
    alias: Breather S OnOff
    sequence:
      - service: switch.turn_on
        entity_id: switch.digma_ir_s_breather_pwr
        
  breather_mb_toggle:
    alias: Breather MB OnOff
    sequence:
      - service: switch.turn_on
        entity_id: switch.digma_ir_mb_breather_pwr

  tv_k_toggle:
    alias: Kitchen TV OnOff
    sequence:
      - service: switch.turn_on
        entity_id: switch.esp32_kitchen_tv_pwr
        
  tv_box_k_toggle:
    alias: Kitchen TV Box OnOff
    sequence:
      - service: switch.turn_on
        entity_id: switch.esp32_kitchen_tvbox_pwr