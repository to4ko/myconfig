{
  "title": "WG Easy",
  "config": {
    "step": {
      "user": {
        "title": "WG Easy",
        "description": "Step 1 of 2: enter the server address (e.g. https://host or https://host/subpath - just the base address, no need to add any API path) and API version. The URL/version will be checked (without any credentials yet) before you're asked for a password or token.",
        "data": {
          "url": "Server address (e.g. https://host or https://host/subpath)",
          "api_version": "wg-easy API version (auto, v14, v15)",
          "verify_ssl": "Verify SSL certificate"
        },
        "data_description": {
          "verify_ssl": "Leave on unless your wg-easy server uses a self-signed certificate you trust. Turning this off skips certificate validation - only use it on a trusted network."
        }
      },
      "credentials": {
        "title": "WG Easy – credentials",
        "description": "Step 2 of 2: detected/selected API version: {api_version}.",
        "data": {
          "token": "API token (v15)",
          "password": "Password (v14)"
        },
        "data_description": {
          "token": "The Bearer Password set under Admin Panel → General → Metrics in the wg-easy web UI. This integration automatically appends /metrics/json to your server address to reach it.",
          "password": "The same password you use to log in to the wg-easy web UI (set via the PASSWORD_HASH environment variable on the wg-easy container)."
        }
      },
      "reconfigure": {
        "title": "Reconfigure WG Easy",
        "description": "Step 1 of 2: update the server address and/or API version.",
        "data": {
          "url": "Server address (e.g. https://host or https://host/subpath)",
          "api_version": "wg-easy API version (auto, v14, v15)",
          "verify_ssl": "Verify SSL certificate"
        },
        "data_description": {
          "verify_ssl": "Leave on unless your wg-easy server uses a self-signed certificate you trust. Turning this off skips certificate validation - only use it on a trusted network."
        }
      },
      "reconfigure_credentials": {
        "title": "Reconfigure WG Easy – credentials",
        "description": "Step 2 of 2: detected/selected API version: {api_version}.",
        "data": {
          "token": "API token (v15)",
          "password": "Password (v14)"
        },
        "data_description": {
          "token": "The Bearer Password set under Admin Panel → General → Metrics in the wg-easy web UI. This integration automatically appends /metrics/json to your server address to reach it.",
          "password": "The same password you use to log in to the wg-easy web UI (set via the PASSWORD_HASH environment variable on the wg-easy container)."
        }
      }
    },
    "error": {
      "cannot_connect": "Couldn't reach this address. Check that the URL is correct, the server is running and reachable from Home Assistant (no firewall/VPN in the way), and that the SSL verification setting matches your certificate (turn it off only for a trusted self-signed certificate).",
      "not_wg_easy": "This address answered, but it doesn't look like a wg-easy server. Double-check the URL, or pick the API version manually instead of auto if you're sure it's correct.",
      "invalid_auth": "The token/password was rejected by the server. Double-check it (Admin Panel → General → Metrics for the v15 token, or your login password for v14) and try again."
    },
    "abort": {
      "already_configured": "This integration is already configured.",
      "wrong_account": "The provided URL does not match this configured instance."
    }
  },
  "options": {
    "step": {
      "init": {
        "title": "WG Easy options",
        "data": {
          "poll_interval": "Polling interval",
          "online_timeout_seconds": "Online timeout (seconds)"
        }
      }
    }
  }
}
